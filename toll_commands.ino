// Open a terminal and run the following commands:



# Navigate to your project folder
cd /path/to/your/project

# Initialize a git repository
git init

# Add your code
git add .

# Commit the code
git commit -m "Initial commit: Arduino Toll Gate System"

# Connect to the GitHub repo (replace with your repo URL)
git remote add origin https://github.com/USERNAME/arduino-toll-gate.git

# Push your code to GitHub
git branch -M main
git push -u origin main
